# Configure only the clients you actually run. Each client requires all
# of its env vars to be set; the MCP server registers tools for a
# client only when its full config is present.

# Required by docker-compose.yml for the HTTP transport.
# Generate a strong bearer token, for example: openssl rand -hex 32
MCP_AUTH_TOKEN=
# Comma-separated bare hostnames clients use to reach the server
# (port-independent; a host:port entry also still matches).
MCP_ALLOWED_HOSTS=

# SABnzbd (usenet)
# Default port: 8080
SABNZBD_URL=http://localhost:8080
SABNZBD_API_KEY=

# qBittorrent (torrents)
# Default WebUI port: 8080 (commonly remapped if SABnzbd is also 8080)
# Requires qBittorrent >= v5.2.0 (WebAPI >= v2.14.1). Generate under
# WebUI options > API Key.
QBITTORRENT_URL=http://localhost:8081
QBITTORRENT_API_KEY=
